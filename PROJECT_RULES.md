# 项目规则

## 项目概述

打造一个完整的家长控制视频播放生态：
- **手机端（BiliPick）**：家长推送B站视频到NAS
- **服务端（fnosserver）**：飞牛NAS上的Docker服务，存储精选视频
- **TV端（bv）**：改造后的播放器，只播放NAS上的视频

## 目录结构

```
BiliTV/
├── BiliPick/                    # Android手机APP（家长端）
│   ├── app/src/main/java/com/example/bilipick/
│   │   ├── MainActivity.kt
│   │   ├── ui/                  # UI组件
│   │   ├── data/                # 数据模型、Repository
│   │   ├── network/             # API接口
│   │   └── util/                # 工具类
│   └── ...
│
├── fnosserver/                  # 飞牛NAS服务端（Docker）
│   ├── app/
│   │   ├── main.py              # FastAPI入口
│   │   ├── models.py            # 数据模型
│   │   └── database.py          # SQLite连接
│   ├── Dockerfile
│   ├── docker-compose.yml
│   └── requirements.txt
│
├── bv/                          # TV播放器（基于BV改造）
│   ├── app/src/main/kotlin/dev/aaa1115910/bv/
│   │   ├── entity/              # 数据类
│   │   ├── network/             # NAS API接口
│   │   ├── screen/curated/      # 精选视频列表
│   │   ├── player/              # 观看时长追踪
│   │   └── ...
│   └── ...
│
├── PARENTAL_CONTROL_DESIGN.md   # 设计方案文档
├── PLAN.md                      # 实施计划
└── PROJECT_RULES.md             # 本项目规则
```

## 开发规则

### 规则1：有疑问先询问

**在执行任何不确定的操作前，必须先向用户确认。**

包括但不限于：
- 技术选型选择（如：用Room还是直接SQLite？）
- 功能实现方式（如：搜索用本地还是服务端？）
- UI设计决策（如：列表用网格还是列表？）
- 删除或修改现有代码
- 添加新的依赖库

**正确示例：**
> "这里有两个方案：A方案用本地缓存，B方案每次都请求服务端。您倾向于哪个？"

### 规则2：重试两次失败转人工

**同一个问题连续尝试两次无法解决，必须停止并询问用户。**

包括但不限于：
- 编译错误无法解决
- 运行时崩溃无法定位
- 网络请求总是失败
- UI显示异常
- 依赖冲突

**正确示例：**
> "这个编译错误我尝试了两种方法都无法解决：1. 升级Gradle版本 2. 清理缓存重编译。建议人工检查，是否需要我暂停等待？"

### 规则3：重要决定先问询

**以下重要决定必须先征得用户同意：**

1. **架构变更**
   - 改变原有的技术方案
   - 引入新的框架/库
   - 修改项目结构

2. **功能增删**
   - 删除已有功能
   - 添加计划外的功能
   - 修改功能逻辑

3. **数据相关**
   - 修改数据模型
   - 删除或清空数据
   - 修改存储方式

4. **配置变更**
   - 修改gradle配置
   - 修改manifest
   - 修改权限申请

5. **外部依赖**
   - 添加新的依赖库
   - 升级依赖版本
   - 引入第三方服务

**正确示例：**
> "我打算添加Retrofit来处理网络请求，需要添加以下依赖：... 是否同意？"

## 开发顺序

按阶段执行，每阶段开始前确认参数：

### 第一阶段：手机端推送功能
1. 搭建NAS服务端（fnosserver）
2. 开发手机APP（BiliPick）
3. 测试推送流程

### 第二阶段：TV端改造
1. 删除无关功能（bv）
2. 实现精选视频列表
3. 实现观看限制

### 第三阶段：打通生态
1. 完善服务端统计功能
2. 完善家长管理APP
3. 集成测试

## 沟通规范

1. **开始前**：确认该阶段的具体需求和参数
2. **进行中**：每完成一个子任务简要汇报
3. **遇到问题时**：立即报告，不隐瞒
4. **完成后**：总结完成内容，确认下一步

## 代码规范

1. **注释**：关键逻辑必须加中文注释
2. **命名**：使用有意义的英文命名
3. **格式**：保持代码整洁，统一缩进
4. **提交**：不擅自提交代码，等待用户确认

## 安全规范

1. **密钥**：不硬编码API密钥，使用配置文件
2. **日志**：不打印敏感信息
3. **权限**：只申请必要的权限
4. **网络**：HTTPS优先，处理网络异常

---

**最后更新**：2024年
**适用范围**：BiliTV项目所有模块
